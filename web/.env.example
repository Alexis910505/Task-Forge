# Copia a `.env` para desarrollo local.
# Vacío = las peticiones van a `/api` y Vite las proxifica a Nest (puerto 3000).
# VITE_API_URL=http://localhost:3000

VITE_API_URL=

# Slug de organización para login (sin campo en pantalla). Por defecto: default
# VITE_ORGANIZATION_SLUG=default
